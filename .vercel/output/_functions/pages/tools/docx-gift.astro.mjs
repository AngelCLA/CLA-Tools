/* empty css                                    */
import { f as createComponent, r as renderTemplate, m as maybeRenderHead, e as createAstro, n as defineScriptVars, o as renderComponent } from '../../chunks/astro/server_CfwuqGYk.mjs';
import 'piccolore';
import { $ as $$Layout } from '../../chunks/Layout_B6TclRQl.mjs';
import 'clsx';
export { renderers } from '../../renderers.mjs';

var __freeze$1 = Object.freeze;
var __defProp$1 = Object.defineProperty;
var __template$1 = (cooked, raw) => __freeze$1(__defProp$1(cooked, "raw", { value: __freeze$1(cooked.slice()) }));
var _a$1;
const $$ExampleCollapsable = createComponent(($$result, $$props, $$slots) => {
  return renderTemplate(_a$1 || (_a$1 = __template$1(["<!-- Formato de ejemplo -->", '<div class="w-full max-w-5xl border-2 mb-4 border-dashed border-zinc-300 dark:border-zinc-700 bg-white dark:bg-zinc-900 rounded-xl p-6"> <button id="toggle-example" class="flex items-center justify-between w-full text-left group cursor-pointer" aria-expanded="false"> <span class="text-lg font-semibold text-purple-600 dark:text-purple-400 flex items-center gap-2"> <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20"> <path d="M9 4.804A7.968 7.968 0 005.5 4c-1.255 0-2.443.29-3.5.804v10A7.969 7.969 0 015.5 14c1.669 0 3.218.51 4.5 1.385A7.962 7.962 0 0114.5 14c1.255 0 2.443.29 3.5.804v-10A7.968 7.968 0 0014.5 4c-1.255 0-2.443.29-3.5.804V12a1 1 0 11-2 0V4.804z"></path> </svg>\nEjemplo de formato en Word (.docx)\n</span> <svg id="toggle-icon" class="w-5 h-5 text-purple-600 dark:text-purple-400 transition-transform duration-200" style="transform: rotate(-90deg);" fill="none" stroke="currentColor" viewBox="0 0 24 24"> <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path> </svg> </button> <div id="example-content" class="overflow-hidden transition-all duration-500 ease-in-out" style="max-height: 0px; opacity: 0; margin-top: 0;"> <p class="text-sm text-zinc-700 dark:text-zinc-300 font-medium mb-4"> <strong>El conversor acepta estos formatos para marcar la respuesta correcta:</strong> </p> <!-- Ejemplo 1 --> <div class="bg-zinc-50 dark:bg-zinc-800 rounded-lg p-4 border border-zinc-200 dark:border-zinc-700"> <p class="text-purple-600 dark:text-purple-400 font-semibold mb-2">\n\u{1F4CC} Formato 1: Texto subrayado con fondo amarillo\n</p> <p class="font-bold text-zinc-900 dark:text-zinc-100">\n\xBFCu\xE1l es la funci\xF3n clave de la Qu\xEDmica Agr\xEDcola?\n</p> <p class="text-zinc-700 dark:text-zinc-300">\nFabricar maquinaria agr\xEDcola innovadora\n</p> <p class="text-zinc-700 dark:text-zinc-300">\nMedir la humedad ambiental y del suelo\n</p> <p class="bg-yellow-200 dark:bg-yellow-500/30 text-zinc-900 dark:text-zinc-100">\nMejorar la productividad del suelo y cultivos\n</p> </div> <!-- Ejemplo 2 --> <div class="bg-zinc-50 dark:bg-zinc-800 rounded-lg p-4 border border-zinc-200 dark:border-zinc-700 mt-3"> <p class="text-purple-600 dark:text-purple-400 font-semibold mb-2">\n\u{1F4CC} Formato 2: Texto con subrayado simple y fondo amarillo\n</p> <p class="font-bold text-zinc-900 dark:text-zinc-100">\nDe los siguientes procesos \xBFCu\xE1l se ve influenciado directamente por la\n        Qu\xEDmica Agr\xEDcola?\n</p> <p class="text-zinc-700 dark:text-zinc-300">\nTranscripci\xF3n gen\xE9tica animal y vegetal\n</p> <p class="text-zinc-700 dark:text-zinc-300">\nReproducci\xF3n animal y vegetal\n</p> <p class="underline bg-yellow-200 dark:bg-yellow-500/30 text-zinc-900 dark:text-zinc-100">\nFotos\xEDntesis\n</p> </div> <!-- Ejemplo 3 --> <div class="bg-zinc-50 dark:bg-zinc-800 rounded-lg p-4 border border-zinc-200 dark:border-zinc-700 mt-3"> <p class="text-purple-600 dark:text-purple-400 font-semibold mb-2">\n\u{1F4CC} Formato 3: Texto con subrayado simple\n</p> <p class="font-bold text-zinc-900 dark:text-zinc-100">\n\xBFCu\xE1l es la unidad m\xE1s com\xFAn para expresar la concentraci\xF3n de\n        nutrientes en fertilizantes?\n</p> <p class="text-zinc-700 dark:text-zinc-300">Decibelios</p> <p class="underline text-zinc-900 dark:text-zinc-100">\nMiligramos por litro (mg/L)\n</p> <p class="text-zinc-700 dark:text-zinc-300">Voltios</p> </div> <!-- Reglas importantes --> <div class="bg-blue-50 dark:bg-blue-900/20 border border-blue-200 dark:border-blue-800 rounded-lg p-4 mt-4"> <p class="text-sm text-zinc-700 dark:text-zinc-300"> <strong class="text-zinc-900 dark:text-zinc-100">Reglas importantes:</strong><br> <span class="block mt-2">\n\u2713 Cada pregunta en <strong>negrita</strong> en un p\xE1rrafo independiente\n</span> <span class="block">\u2713 Cada respuesta en su propio p\xE1rrafo</span> <span class="block">\n\u2713 Marca la respuesta correcta con <u>subrayado</u> o resaltado amarillo\n</span> <span class="block">\n\u2717 Evita listas con vi\xF1etas (usa p\xE1rrafos simples)\n</span> </p> </div> </div> </div> <script>\n  document.addEventListener("DOMContentLoaded", function () {\n    const toggleButton = document.getElementById("toggle-example");\n    const exampleContent = document.getElementById("example-content");\n    const toggleIcon = document.getElementById("toggle-icon");\n\n    if (toggleButton && exampleContent && toggleIcon) {\n      toggleButton.addEventListener("click", function () {\n        const isCollapsed = exampleContent.style.maxHeight === "0px";\n\n        if (isCollapsed) {\n          exampleContent.style.maxHeight = "2000px";\n          exampleContent.style.opacity = "1";\n          exampleContent.style.marginTop = "1rem";\n          toggleIcon.style.transform = "rotate(0deg)";\n          toggleButton.setAttribute("aria-expanded", "true");\n        } else {\n          exampleContent.style.maxHeight = "0px";\n          exampleContent.style.opacity = "0";\n          exampleContent.style.marginTop = "0";\n          toggleIcon.style.transform = "rotate(-90deg)";\n          toggleButton.setAttribute("aria-expanded", "false");\n        }\n      });\n    }\n  });\n<\/script>'])), maybeRenderHead());
}, "C:/dev/Astro/cla-tools/src/components/ExampleCollapsable.astro", void 0);

const commonScript = "/**\r\n * Utilidades comunes para todas las páginas\r\n */\r\n\r\nconsole.log('common.js cargado');\r\n\r\n// Configuración global\r\nconst AppConfig = {\r\n    animations: {\r\n        duration: 300,\r\n        easing: 'ease-out'\r\n    },\r\n    messages: {\r\n        showDuration: 5000,\r\n        successDuration: 3000\r\n    }\r\n};\r\n\r\n// Utilidades para mensajes\r\nclass MessageHandler {\r\n    static show(element, type = 'info', duration = AppConfig.messages.showDuration) {\r\n        if (!element) {\r\n            console.log('Elemento de mensaje no encontrado');\r\n            return;\r\n        }\r\n        \r\n        element.classList.add('show');\r\n        \r\n        if (type === 'success') {\r\n            duration = AppConfig.messages.successDuration;\r\n        }\r\n        \r\n        setTimeout(() => {\r\n            element.classList.remove('show');\r\n        }, duration);\r\n    }\r\n\r\n    static showSuccess(message, customElement = null) {\r\n        console.log('Mostrando mensaje de éxito:', message);\r\n        const element = customElement || document.getElementById('successMessage');\r\n        if (element) {\r\n            element.textContent = `✅ ${message}`;\r\n            this.show(element, 'success');\r\n        }\r\n    }\r\n\r\n    static showError(message, customElement = null) {\r\n        console.log('Mostrando mensaje de error:', message);\r\n        const element = customElement || document.getElementById('errorMessage');\r\n        if (element) {\r\n            element.textContent = `❌ ${message}`;\r\n            this.show(element, 'error');\r\n        }\r\n    }\r\n\r\n    static showWarning(message, customElement = null) {\r\n        console.log('Mostrando mensaje de advertencia:', message);\r\n        const element = customElement || document.getElementById('warningMessage');\r\n        if (element) {\r\n            element.textContent = `⚠️ ${message}`;\r\n            this.show(element, 'warning');\r\n        }\r\n    }\r\n}\r\n\r\n// Utilidades para archivos\r\nclass FileHandler {\r\n    static formatFileSize(bytes) {\r\n        if (bytes === 0) return '0 Bytes';\r\n        \r\n        const k = 1024;\r\n        const sizes = ['Bytes', 'KB', 'MB', 'GB'];\r\n        const i = Math.floor(Math.log(bytes) / Math.log(k));\r\n        \r\n        return parseFloat((bytes / Math.pow(k, i)).toFixed(2)) + ' ' + sizes[i];\r\n    }\r\n\r\n    static readFileAsArrayBuffer(file) {\r\n        return new Promise((resolve, reject) => {\r\n            const reader = new FileReader();\r\n            \r\n            reader.onload = (e) => resolve(e.target.result);\r\n            reader.onerror = () => reject(new Error('Error al leer el archivo'));\r\n            \r\n            reader.readAsArrayBuffer(file);\r\n        });\r\n    }\r\n\r\n    static downloadFile(content, filename, type = 'text/plain') {\r\n        const blob = new Blob([content], { type: `${type}; charset=utf-8` });\r\n        const url = window.URL.createObjectURL(blob);\r\n        \r\n        const a = document.createElement('a');\r\n        a.href = url;\r\n        a.download = filename;\r\n        document.body.appendChild(a);\r\n        a.click();\r\n        document.body.removeChild(a);\r\n        \r\n        window.URL.revokeObjectURL(url);\r\n    }\r\n}\r\n\r\n// Utilidades para DOM\r\nclass DOMUtils {\r\n    static setupDragAndDrop(uploadArea, fileInput, onFilesSelected, acceptedTypes = []) {\r\n        console.log('Configurando drag and drop');\r\n        \r\n        if (!uploadArea || !fileInput) {\r\n            console.error('uploadArea o fileInput no encontrado');\r\n            return;\r\n        }\r\n\r\n        uploadArea.addEventListener('click', () => {\r\n            console.log('Click en uploadArea');\r\n            fileInput.click();\r\n        });\r\n\r\n        uploadArea.addEventListener('dragover', (e) => {\r\n            e.preventDefault();\r\n            uploadArea.classList.add('dragover');\r\n        });\r\n\r\n        uploadArea.addEventListener('dragleave', () => {\r\n            uploadArea.classList.remove('dragover');\r\n        });\r\n\r\n        uploadArea.addEventListener('drop', (e) => {\r\n            e.preventDefault();\r\n            uploadArea.classList.remove('dragover');\r\n\r\n            const files = Array.from(e.dataTransfer.files);\r\n            this.handleFileSelection(files, onFilesSelected, acceptedTypes);\r\n        });\r\n\r\n        fileInput.addEventListener('change', (e) => {\r\n            console.log('Archivo seleccionado');\r\n            const files = Array.from(e.target.files);\r\n            this.handleFileSelection(files, onFilesSelected, acceptedTypes);\r\n        });\r\n    }\r\n\r\n    static handleFileSelection(files, callback, acceptedTypes = []) {\r\n        console.log('Procesando archivos seleccionados:', files);\r\n        \r\n        if (acceptedTypes.length === 0) {\r\n            callback(files);\r\n            return;\r\n        }\r\n\r\n        const validFiles = files.filter(file => {\r\n            return acceptedTypes.some(type => {\r\n                if (type.startsWith('.')) {\r\n                    return file.name.toLowerCase().endsWith(type.toLowerCase());\r\n                }\r\n                return file.type === type;\r\n            });\r\n        });\r\n\r\n        if (validFiles.length === 0) {\r\n            MessageHandler.showError(`Por favor, selecciona archivos válidos (${acceptedTypes.join(', ')})`);\r\n            return;\r\n        }\r\n\r\n        callback(validFiles);\r\n    }\r\n\r\n    static copyToClipboard(text, button = null) {\r\n        if (navigator.clipboard) {\r\n            navigator.clipboard.writeText(text).then(() => {\r\n                this.showCopySuccess(button);\r\n            }).catch(err => {\r\n                console.error('Error al copiar:', err);\r\n                this.fallbackCopyToClipboard(text, button);\r\n            });\r\n        } else {\r\n            this.fallbackCopyToClipboard(text, button);\r\n        }\r\n    }\r\n\r\n    static fallbackCopyToClipboard(text, button) {\r\n        const textArea = document.createElement('textarea');\r\n        textArea.value = text;\r\n        document.body.appendChild(textArea);\r\n        textArea.select();\r\n        try {\r\n            document.execCommand('copy');\r\n            this.showCopySuccess(button);\r\n        } catch (err) {\r\n            console.error('Error al copiar (fallback):', err);\r\n        }\r\n        document.body.removeChild(textArea);\r\n    }\r\n\r\n    static showCopySuccess(button) {\r\n        if (!button) return;\r\n\r\n        const originalText = button.textContent;\r\n        button.textContent = '✅ ¡Copiado!';\r\n        button.style.background = 'var(--success-color)';\r\n\r\n        setTimeout(() => {\r\n            button.textContent = originalText;\r\n            button.style.background = '';\r\n        }, 2000);\r\n    }\r\n}\r\n\r\n// Clase base para componentes de página\r\nclass PageComponent {\r\n    constructor() {\r\n        console.log('Inicializando PageComponent');\r\n        this.elements = {};\r\n        this.init();\r\n    }\r\n\r\n    init() {\r\n        this.bindElements();\r\n        this.attachEvents();\r\n    }\r\n\r\n    bindElements() {\r\n        // Override en clases hijas\r\n    }\r\n\r\n    attachEvents() {\r\n        // Override en clases hijas\r\n    }\r\n\r\n    resetInterface() {\r\n        // Override en clases hijas\r\n    }\r\n}\r\n\r\n// Función para purgar caché\r\nfunction clearCache() {\r\n    try {\r\n        // Limpiar localStorage\r\n        localStorage.clear();\r\n        \r\n        // Limpiar sessionStorage\r\n        sessionStorage.clear();\r\n        \r\n        // Limpiar caché del navegador (si es compatible)\r\n        if ('caches' in window) {\r\n            caches.keys().then(function(names) {\r\n                for (let name of names) {\r\n                    caches.delete(name);\r\n                }\r\n            });\r\n        }\r\n        \r\n        // Mostrar mensaje de confirmación\r\n        showCacheMessage('✅ Caché purgado exitosamente', 'success');\r\n        \r\n        // Opcional: recargar la página después de un delay\r\n        setTimeout(() => {\r\n            window.location.reload(true);\r\n        }, 1500);\r\n        \r\n    } catch (error) {\r\n        console.error('Error al purgar caché:', error);\r\n        showCacheMessage('❌ Error al purgar caché', 'error');\r\n    }\r\n}\r\n\r\n// Función para mostrar mensajes\r\nfunction showCacheMessage(message, type) {\r\n    // Crear elemento del mensaje\r\n    const messageEl = document.createElement('div');\r\n    messageEl.className = `cache-message cache-message-${type}`;\r\n    messageEl.textContent = message;\r\n    \r\n    // Estilos del mensaje\r\n    messageEl.style.cssText = `\r\n        position: fixed;\r\n        top: 20px;\r\n        right: 20px;\r\n        padding: 12px 20px;\r\n        border-radius: 6px;\r\n        color: white;\r\n        font-weight: 500;\r\n        z-index: 10000;\r\n        transform: translateX(100%);\r\n        transition: transform 0.3s ease;\r\n        ${type === 'success' ? 'background: linear-gradient(135deg, #27ae60, #2ecc71);' : 'background: linear-gradient(135deg, #e74c3c, #c0392b);'}\r\n    `;\r\n    \r\n    // Agregar al body\r\n    document.body.appendChild(messageEl);\r\n    \r\n    // Animación de entrada\r\n    setTimeout(() => {\r\n        messageEl.style.transform = 'translateX(0)';\r\n    }, 100);\r\n    \r\n    // Remover después de 3 segundos\r\n    setTimeout(() => {\r\n        messageEl.style.transform = 'translateX(100%)';\r\n        setTimeout(() => {\r\n            if (messageEl.parentNode) {\r\n                messageEl.parentNode.removeChild(messageEl);\r\n            }\r\n        }, 300);\r\n    }, 3000);\r\n}\r\n\r\n// Event listener para el botón de purgar caché\r\ndocument.addEventListener('DOMContentLoaded', function() {\r\n    const clearCacheBtn = document.getElementById('clearCacheBtn');\r\n    \r\n    if (clearCacheBtn) {\r\n        clearCacheBtn.addEventListener('click', function(e) {\r\n            e.preventDefault();\r\n            \r\n            // Confirmación antes de purgar\r\n            if (confirm('¿Estás seguro de que deseas purgar toda la caché? Esto puede afectar el rendimiento temporalmente.')) {\r\n                clearCache();\r\n            }\r\n        });\r\n    }\r\n});\r\n\r\n// Exportar para uso global\r\nwindow.AppConfig = AppConfig;\r\nwindow.MessageHandler = MessageHandler;\r\nwindow.FileHandler = FileHandler;\r\nwindow.DOMUtils = DOMUtils;\r\nwindow.PageComponent = PageComponent;\r\n\r\nconsole.log('common.js: Variables globales exportadas');\r\n";

const docxConverterScript = "console.log('docx-gift.js cargado');\r\n\r\nfunction onConversionSuccess() {\r\n    console.log('🎉 Registrando conversión exitosa...');\r\n    \r\n    fetch(\"/api/tool-use-real\", {\r\n        method: \"POST\",\r\n        headers: { \"Content-Type\": \"application/json\" },\r\n        body: JSON.stringify({ tool_slug: \"docx-to-gift\" }),\r\n    })\r\n    .then(res => res.json())\r\n    .then(data => console.log('✅ Conversión registrada:', data))\r\n    .catch(err => console.error('❌ Error:', err));\r\n}\r\n\r\n\r\n// Verificar dependencias\r\nif (typeof mammoth === 'undefined') {\r\n    console.error('Mammoth.js no está cargado');\r\n}\r\n\r\nclass DocxToGiftConverter {\r\n    constructor() {\r\n        console.log('Inicializando DocxToGiftConverter');\r\n        this.processedContent = '';\r\n        this.fileName = '';\r\n        this.debugInfo = '';\r\n        this.elements = {};\r\n        this.highlightedTexts = new Set(); // Almacenar textos con highlighting\r\n        \r\n        // Inicializar cuando el DOM esté listo\r\n        if (document.readyState === 'loading') {\r\n            document.addEventListener('DOMContentLoaded', () => this.init());\r\n        } else {\r\n            this.init();\r\n        }\r\n    }\r\n\r\n    init() {\r\n        // console.log('Inicializando converter');\r\n        this.bindElements();\r\n        this.attachEvents();\r\n    }\r\n\r\n    bindElements() {\r\n        // console.log('Vinculando elementos del DOM');\r\n        this.elements = {\r\n            uploadArea: document.getElementById('upload-area'),\r\n            fileInput: document.getElementById('file-input'),\r\n            outputArea: document.getElementById('output-area'),\r\n            giftOutput: document.getElementById('gift-output'),\r\n            loading: document.getElementById('loading'),\r\n            fileInfo: document.getElementById('file-info'),\r\n            fileName: document.getElementById('file-name'),\r\n            removeFile: document.getElementById('remove-file'),\r\n            debugArea: document.getElementById('debugArea'),\r\n            debugContent: document.getElementById('debugContent'),\r\n            previewSection: document.getElementById('preview-section'),\r\n            questionsPreview: document.getElementById('questions-preview'),\r\n            questionCount: document.getElementById('question-count'),\r\n            answerCount: document.getElementById('answer-count'),\r\n            correctCount: document.getElementById('correct-count'),\r\n            successMessage: document.getElementById('success-message'),\r\n            errorMessage: document.getElementById('error-message'),\r\n            copyBtn: document.getElementById('copy-btn'),\r\n            downloadBtn: document.getElementById('download-btn')\r\n        };\r\n\r\n        // Verificar que los elementos existan\r\n        Object.keys(this.elements).forEach(key => {\r\n            if (!this.elements[key]) {\r\n                console.warn(`Elemento ${key} no encontrado`);\r\n            }\r\n        });\r\n    }\r\n\r\n    attachEvents() {\r\n        // console.log('Adjuntando eventos');\r\n        \r\n        // Setup drag and drop\r\n        if (this.elements.uploadArea && this.elements.fileInput) {\r\n            DOMUtils.setupDragAndDrop(\r\n                this.elements.uploadArea,\r\n                this.elements.fileInput,\r\n                (files) => this.handleFiles(files),\r\n                ['.docx']\r\n            );\r\n        }\r\n\r\n        // Remove file event\r\n        if (this.elements.removeFile) {\r\n            this.elements.removeFile.addEventListener('click', () => {\r\n                console.log('Removiendo archivo');\r\n                this.resetInterface();\r\n            });\r\n        }\r\n\r\n        // Copy button event\r\n        if (this.elements.copyBtn) {\r\n            this.elements.copyBtn.addEventListener('click', () => {\r\n                console.log('Copiando al portapapeles');\r\n                if (this.elements.giftOutput && this.elements.giftOutput.value) {\r\n                    DOMUtils.copyToClipboard(this.elements.giftOutput.value, this.elements.copyBtn);\r\n                } else {\r\n                    MessageHandler.showError('No hay contenido para copiar');\r\n                }\r\n            });\r\n        }\r\n\r\n        // Download button event\r\n        if (this.elements.downloadBtn) {\r\n            this.elements.downloadBtn.addEventListener('click', () => {\r\n                console.log('Descargando archivo GIFT');\r\n                if (!this.processedContent) {\r\n                    MessageHandler.showError('No hay contenido para descargar');\r\n                    return;\r\n                }\r\n\r\n                FileHandler.downloadFile(\r\n                    this.processedContent,\r\n                    `${this.fileName || 'preguntas'}.gift`,\r\n                    'text/plain'\r\n                );\r\n            });\r\n        }\r\n    }\r\n\r\n    handleFiles(files) {\r\n        // console.log('Manejando archivos:', files);\r\n        if (files.length === 0) return;\r\n        \r\n        const file = files[0];\r\n        if (!file.name.toLowerCase().endsWith('.docx')) {\r\n            MessageHandler.showError('Por favor, selecciona un archivo DOCX');\r\n            return;\r\n        }\r\n\r\n        this.processFile(file);\r\n    }\r\n\r\n    async processFile(file) {\r\n        // console.log('Procesando archivo:', file.name);\r\n        \r\n        this.fileName = file.name.replace('.docx', '');\r\n        \r\n        if (this.elements.fileName) {\r\n            this.elements.fileName.textContent = file.name;\r\n        }\r\n        \r\n        if (this.elements.fileInfo) {\r\n            this.elements.fileInfo.classList.remove('hidden');\r\n        }\r\n\r\n        this.showLoading(true);\r\n        this.hideMessages();\r\n\r\n        try {\r\n            const arrayBuffer = await FileHandler.readFileAsArrayBuffer(file);\r\n// console.log('Archivo leído, procesando con Mammoth...');\r\n            \r\n            // Extraer highlighting del XML del DOCX\r\n            await this.extractHighlightedTexts(arrayBuffer);\r\n            \r\n            const options = {\r\n                styleMap: [\r\n                    \"p[style-name='Heading 1'] => h1:fresh\",\r\n                    \"p[style-name='Heading 2'] => h2:fresh\",\r\n                    \"u => u\",\r\n                    // Capturar runs con highlight (resaltado) de Word\r\n                    \"r[style-name='Highlight'] => span.highlight\",\r\n                    // Alternativas para capturar highlight\r\n                    \"r:highlight => span:css(background-color: yellow)\",\r\n                ]\r\n            };\r\n\r\n            const result = await mammoth.convertToHtml({ arrayBuffer }, options);\r\n            const html = result.value;\r\n            const messages = result.messages;\r\n\r\n            // console.log('HTML generado por Mammoth:', html.substring(0, 200) + '...');\r\n\r\n            this.debugInfo = \"=== MAMMOTH MESSAGES ===\\n\";\r\n            messages.forEach(msg => {\r\n                this.debugInfo += `${msg.type}: ${msg.message}\\n`;\r\n            });\r\n            this.debugInfo += \"\\n=== HTML GENERADO ===\\n\";\r\n            this.debugInfo += html.substring(0, 1000) + (html.length > 1000 ? \"...\\n\" : \"\\n\");\r\n\r\n            this.processDocument(html);\r\n        } catch (error) {\r\n            console.error('Error procesando archivo:', error);\r\n            this.showLoading(false);\r\n            MessageHandler.showError('Error al leer el archivo DOCX: ' + error.message);\r\n        }\r\n    }\r\n\r\n    async extractHighlightedTexts(arrayBuffer) {\r\n        try {\r\n            if (typeof JSZip === 'undefined') {\r\n                console.warn('JSZip no está disponible, saltando extracción de highlighting');\r\n                return;\r\n            }\r\n\r\n            const zip = await JSZip.loadAsync(arrayBuffer);\r\n            const docXml = await zip.file('word/document.xml').async('string');\r\n            \r\n            // console.log('=== EXTRAYENDO HIGHLIGHTING ===');\r\n            \r\n            // Regex correcta: Buscar <w:r> que contenga <w:highlight w:val=\"yellow\"/> y extraer el texto\r\n            const highlightPattern = /<w:r[^>]*>[\\s\\S]*?<w:highlight\\s+w:val=\"yellow\"[\\s\\S]*?<w:t[^>]*>([^<]+)<\\/w:t>[\\s\\S]*?<\\/w:r>/gi;\r\n            \r\n            let match;\r\n            while ((match = highlightPattern.exec(docXml)) !== null) {\r\n                const text = match[1]?.trim();\r\n                if (text) {\r\n                    this.highlightedTexts.add(text);\r\n                    // console.log(`✓ Highlighting detectado: \"${text}\"`);\r\n                }\r\n            }\r\n\r\n            // console.log('Textos con highlighting final:', Array.from(this.highlightedTexts));\r\n            // console.log('Total encontrados:', this.highlightedTexts.size);\r\n        } catch (error) {\r\n            console.warn('Error extrayendo highlighting del DOCX:', error);\r\n        }\r\n    }\r\n\r\n    processDocument(html) {\r\n        // console.log('Procesando documento HTML');\r\n        try {\r\n            const tempDiv = document.createElement('div');\r\n            tempDiv.innerHTML = html;\r\n\r\n            const documentStructure = this.extractDocumentStructure(tempDiv);\r\n            // console.log('Estructura extraída:', documentStructure);\r\n            \r\n            const giftContent = this.generateGiftFormat(documentStructure);\r\n            // console.log('Contenido GIFT generado');\r\n\r\n            this.processedContent = giftContent;\r\n            this.updateStatistics(documentStructure);\r\n            this.generatePreview(documentStructure);\r\n\r\n            this.showResults(giftContent);\r\n            MessageHandler.showSuccess('¡Archivo convertido exitosamente!');\r\n            onConversionSuccess();\r\n\r\n        } catch (error) {\r\n            console.error('Error procesando documento:', error);\r\n            this.debugInfo += `\\nERROR: ${error.message}\\n${error.stack}\\n`;\r\n            \r\n            if (this.elements.debugContent) {\r\n                this.elements.debugContent.textContent = this.debugInfo;\r\n            }\r\n            if (this.elements.debugArea) {\r\n                this.elements.debugArea.classList.add('show');\r\n            }\r\n            \r\n            this.showLoading(false);\r\n            MessageHandler.showError('Error al procesar el documento. Ver información de depuración.');\r\n        }\r\n    }\r\n\r\n    extractDocumentStructure(htmlElement) {\r\n        // console.log('Extrayendo estructura del documento');\r\n        const structure = [];\r\n        const paragraphs = htmlElement.querySelectorAll('p');\r\n        \r\n        let currentQuestion = null;\r\n        let currentAnswers = [];\r\n\r\n        paragraphs.forEach((p, index) => {\r\n            const text = p.textContent.trim();\r\n            if (!text) return;\r\n\r\n            this.debugInfo += `\\nPárrafo ${index}: \"${text.substring(0, 50)}...\"\\n`;\r\n\r\n            const boldElements = p.querySelectorAll('strong, b');\r\n            const hasStrongContent = boldElements.length > 0 &&\r\n                Array.from(boldElements).some(b => b.textContent.trim().length > 5);\r\n\r\n            if (hasStrongContent) {\r\n                if (currentQuestion && currentAnswers.length > 0) {\r\n                    structure.push({\r\n                        question: currentQuestion,\r\n                        answers: currentAnswers\r\n                    });\r\n                }\r\n\r\n                currentQuestion = text;\r\n                currentAnswers = [];\r\n                this.debugInfo += `  → PREGUNTA DETECTADA\\n`;\r\n\r\n            } else if (text && currentQuestion && text.length > 1) {\r\n                const isCorrect = this.isAnswerCorrect(p, text);\r\n                \r\n                // console.log(`Respuesta: \"${text.substring(0, 50)}...\" - HTML:`, p.outerHTML.substring(0, 300));\r\n                this.debugInfo += `  HTML de la respuesta: ${p.outerHTML.substring(0, 200)}\\n`;\r\n                \r\n                currentAnswers.push({\r\n                    text: text,\r\n                    isCorrect: isCorrect\r\n                });\r\n            }\r\n        });\r\n\r\n        if (currentQuestion && currentAnswers.length > 0) {\r\n            structure.push({\r\n                question: currentQuestion,\r\n                answers: currentAnswers\r\n            });\r\n        }\r\n\r\n        // Validar respuestas correctas\r\n        structure.forEach((item, index) => {\r\n            const correctAnswers = item.answers.filter(a => a.isCorrect);\r\n            if (correctAnswers.length === 0 && item.answers.length > 0) {\r\n                item.answers[0].isCorrect = true;\r\n                this.debugInfo += `\\nADVERTENCIA: Pregunta ${index + 1} no tenía respuesta correcta. Se marcó la primera.\\n`;\r\n            }\r\n        });\r\n\r\n        // console.log('Estructura final:', structure);\r\n        return structure;\r\n    }\r\n\r\n    isAnswerCorrect(paragraph, text) {\r\n        // 1. Verificar si está en los textos con highlighting del XML\r\n        if (this.highlightedTexts.has(text.trim())) {\r\n            this.debugInfo += `  → RESPUESTA CORRECTA (highlighting del XML): \"${text}\"\\n`;\r\n            // console.log(`✓ Highlighting detectado: \"${text}\"`);\r\n            return true;\r\n        }\r\n\r\n        // 2. Verificar elementos subrayados\r\n        const underlineElements = paragraph.querySelectorAll('u');\r\n        if (underlineElements.length > 0) {\r\n            for (const u of underlineElements) {\r\n                const underlineText = u.textContent.trim();\r\n                if (underlineText === text || text.includes(underlineText)) {\r\n                    this.debugInfo += `  → RESPUESTA CORRECTA (subrayada): \"${text}\"\\n`;\r\n                    return true;\r\n                }\r\n            }\r\n        }\r\n\r\n        // 3. Verificar elementos con resaltado (span.highlight)\r\n        const highlightElements = paragraph.querySelectorAll('span.highlight');\r\n        if (highlightElements.length > 0) {\r\n            for (const span of highlightElements) {\r\n                const highlightText = span.textContent.trim();\r\n                if (highlightText === text || text.includes(highlightText)) {\r\n                    this.debugInfo += `  → RESPUESTA CORRECTA (resaltada span.highlight): \"${text}\"\\n`;\r\n                    return true;\r\n                }\r\n            }\r\n        }\r\n\r\n        // 4. Verificar estilo de subrayado\r\n        const style = paragraph.getAttribute('style') || '';\r\n        if (style.includes('text-decoration') && style.includes('underline')) {\r\n            this.debugInfo += `  → RESPUESTA CORRECTA (estilo): \"${text}\"\\n`;\r\n            return true;\r\n        }\r\n\r\n        // 5. Verificar spans con subrayado\r\n        const spans = paragraph.querySelectorAll('span');\r\n        for (const span of spans) {\r\n            const spanStyle = span.getAttribute('style') || '';\r\n            if (spanStyle.includes('text-decoration') && spanStyle.includes('underline')) {\r\n                if (span.textContent.trim() === text || text.includes(span.textContent.trim())) {\r\n                    this.debugInfo += `  → RESPUESTA CORRECTA (span): \"${text}\"\\n`;\r\n                    return true;\r\n                }\r\n            }\r\n        }\r\n\r\n        // 6. Verificar resaltado (background-color) típico de Word\r\n        // Caso 1: el párrafo o sus hijos tienen background-color indicando resaltado\r\n        const hasParagraphHighlight = /background(-color)?:\\s*(yellow|#[0-9a-fA-F]{3,6}|rgb\\s*\\(\\s*\\d+\\s*,\\s*\\d+\\s*,\\s*\\d+\\s*\\))/i.test(style);\r\n        if (hasParagraphHighlight) {\r\n            this.debugInfo += `  → RESPUESTA CORRECTA (resaltado en párrafo): \"${text}\"\\n`;\r\n            return true;\r\n        }\r\n\r\n        // 7. Verificar spans con background-color (resaltado amarillo/highlight)\r\n        for (const span of spans) {\r\n            const spanStyle = span.getAttribute('style') || '';\r\n            // Detectar background-color en cualquier variante\r\n            if (spanStyle.toLowerCase().includes('background-color') || spanStyle.toLowerCase().includes('background')) {\r\n                const spanText = span.textContent.trim();\r\n                if (spanText && (spanText === text || text.includes(spanText))) {\r\n                    this.debugInfo += `  → RESPUESTA CORRECTA (span background-color): \"${text}\"\\n`;\r\n                    // console.log(`✓ Detectada como correcta por background-color: \"${spanText}\"`);\r\n                    return true;\r\n                }\r\n            }\r\n        }\r\n\r\n        this.debugInfo += `  → respuesta incorrecta: \"${text}\"\\n`;\r\n        return false;\r\n    }\r\n\r\n    generateGiftFormat(structure) {\r\n        // console.log('Generando formato GIFT');\r\n        let giftContent = '';\r\n\r\n        structure.forEach(item => {\r\n            const cleanQuestion = this.escapeGiftCharacters(item.question.replace(/\\*\\*/g, '').trim());\r\n            \r\n            giftContent += `${cleanQuestion} {\\n`;\r\n\r\n            item.answers.forEach(answer => {\r\n                const prefix = answer.isCorrect ? '=' : '~';\r\n                const cleanAnswer = this.escapeGiftCharacters(answer.text);\r\n                giftContent += `  ${prefix}${cleanAnswer}\\n`;\r\n            });\r\n\r\n            giftContent += '}\\n\\n';\r\n        });\r\n\r\n        return giftContent;\r\n    }\r\n\r\n    escapeGiftCharacters(text) {\r\n        return text\r\n            .replace(/~/g, '\\\\~')\r\n            .replace(/=/g, '\\\\=')\r\n            .replace(/:/g, '\\\\:')\r\n            .replace(/{/g, '\\\\{')\r\n            .replace(/}/g, '\\\\}');\r\n    }\r\n\r\n    generatePreview(structure) {\r\n        // console.log('Generando vista previa');\r\n        if (!this.elements.questionsPreview) return;\r\n\r\n        let previewHTML = '';\r\n\r\n        structure.forEach((item, index) => {\r\n            previewHTML += `\r\n                <div class=\"bg-zinc-50 dark:bg-zinc-900/50 rounded-lg p-4 border border-zinc-200 dark:border-zinc-700 mb-3 mx-6\">\r\n                    <div class=\"font-semibold text-zinc-900 dark:text-zinc-100 mb-3\">${index + 1}. ${item.question}</div>\r\n                    ${item.answers.map(answer => `\r\n                        <div class=\"flex items-start gap-2 py-1 text-sm ${answer.isCorrect ? 'text-green-700 dark:text-green-400 font-medium' : 'text-zinc-600 dark:text-zinc-400'}\">\r\n                            <span class=\"mt-0.5\">${answer.isCorrect ? '✓' : '○'}</span>\r\n                            <span>${answer.text}</span>\r\n                        </div>\r\n                    `).join('')}\r\n                </div>\r\n            `;\r\n        });\r\n\r\n        this.elements.questionsPreview.innerHTML = previewHTML;\r\n    }\r\n\r\n    updateStatistics(structure) {\r\n        // console.log('Actualizando estadísticas');\r\n        const questionCount = structure.length;\r\n        let totalAnswers = 0;\r\n        let correctAnswers = 0;\r\n\r\n        structure.forEach(item => {\r\n            totalAnswers += item.answers.length;\r\n            correctAnswers += item.answers.filter(a => a.isCorrect).length;\r\n        });\r\n\r\n        if (this.elements.questionCount) {\r\n            this.elements.questionCount.textContent = questionCount;\r\n        }\r\n        if (this.elements.answerCount) {\r\n            this.elements.answerCount.textContent = totalAnswers;\r\n        }\r\n        if (this.elements.correctCount) {\r\n            this.elements.correctCount.textContent = correctAnswers;\r\n        }\r\n    }\r\n\r\n    showResults(giftContent) {\r\n        // console.log('Mostrando resultados');\r\n        this.showLoading(false);\r\n        \r\n        if (this.elements.giftOutput) {\r\n            this.elements.giftOutput.value = giftContent;\r\n        }\r\n        if (this.elements.outputArea) {\r\n            this.elements.outputArea.classList.remove('hidden');\r\n        }\r\n        if (this.elements.previewSection) {\r\n            this.elements.previewSection.classList.remove('hidden');\r\n        }\r\n    }\r\n\r\n    showLoading(show) {\r\n        if (show) {\r\n            if (this.elements.loading) {\r\n                this.elements.loading.classList.remove('hidden');\r\n            }\r\n            if (this.elements.outputArea) {\r\n                this.elements.outputArea.classList.add('hidden');\r\n            }\r\n        } else {\r\n            if (this.elements.loading) {\r\n                this.elements.loading.classList.add('hidden');\r\n            }\r\n        }\r\n    }\r\n\r\n    hideMessages() {\r\n        if (this.elements.successMessage) {\r\n            this.elements.successMessage.classList.add('hidden');\r\n        }\r\n        if (this.elements.errorMessage) {\r\n            this.elements.errorMessage.classList.add('hidden');\r\n        }\r\n    }\r\n\r\n    resetInterface() {\r\n        // console.log('Reseteando interfaz');\r\n        if (this.elements.fileInput) {\r\n            this.elements.fileInput.value = '';\r\n        }\r\n        if (this.elements.fileInfo) {\r\n            this.elements.fileInfo.classList.add('hidden');\r\n        }\r\n        if (this.elements.outputArea) {\r\n            this.elements.outputArea.classList.add('hidden');\r\n        }\r\n        this.hideMessages();\r\n        if (this.elements.previewSection) {\r\n            this.elements.previewSection.classList.add('hidden');\r\n        }\r\n        if (this.elements.giftOutput) {\r\n            this.elements.giftOutput.value = '';\r\n        }\r\n        this.processedContent = '';\r\n        this.fileName = '';\r\n        this.debugInfo = '';\r\n        this.highlightedTexts = new Set(); // Reset highlighting\r\n    }\r\n\r\n    \r\n}\r\n\r\n// Inicializar converter\r\n// console.log('Creando instancia del converter');\r\nwindow.docxConverter = new DocxToGiftConverter();\r\n";

var __freeze = Object.freeze;
var __defProp = Object.defineProperty;
var __template = (cooked, raw) => __freeze(__defProp(cooked, "raw", { value: __freeze(cooked.slice()) }));
var _a;
const $$Astro = createAstro("https://tools.claangel.site");
const $$DocxGift = createComponent(($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro, $$props, $$slots);
  Astro2.self = $$DocxGift;
  return renderTemplate(_a || (_a = __template(["", ' <!-- Cargar Mammoth.js desde CDN --> <script src="https://cdn.jsdelivr.net/npm/mammoth@1.6.0/mammoth.browser.min.js"><\/script> <!-- Cargar JSZip para leer el contenido interno del DOCX --> <script src="https://cdn.jsdelivr.net/npm/jszip@3.10.1/dist/jszip.min.js"><\/script> <!-- Cargar common.js primero para las utilidades --> <script>(function(){', "\n  eval(commonScript);\n})();<\/script> <!-- Cargar el conversor DOCX a GIFT --> <script>(function(){", "\n  eval(converterScript);\n})();<\/script>"])), renderComponent($$result, "Layout", $$Layout, { "title": "Convertir DOCX a GIFT para Moodle | CLA Tools", "description": "Convierte documentos de Word (.docx) al formato GIFT para importar preguntas en Moodle. Herramienta gratuita y f\xE1cil de usar.", "keywords": "docx a gift, word a gift, moodle, importar preguntas moodle, gift format, converter" }, { "default": ($$result2) => renderTemplate` ${maybeRenderHead()}<div class="flex flex-col min-h-full items-center justify-center"> <div class="w-full max-w-6xl shadow-2xl p-4 sm:p-6 rounded-lg flex flex-col items-center justify-center my-8 sm:my-12 md:my-20 mx-4"> <h3 class="text-3xl sm:text-4xl md:text-5xl font-bold text-purple-600 dark:text-purple-400 mb-6 text-center">
Docx a Gift
</h3> <p class="mb-6 text-base sm:text-lg md:text-xl font-semibold text-purple-600 dark:text-purple-400 text-center px-4">
Convierte tus preguntas de Word al formato GIFT de Moodle
        automáticamente
</p> <p class="mb-6 text-sm sm:text-base text-center px-4">
Formato: Preguntas en <strong>negrita</strong>, respuesta correcta
<u>subrayada</u> </p> ${renderComponent($$result2, "ExampleCollapsable", $$ExampleCollapsable, {})} <!-- Upload Area --> <div id="upload-area" class="w-full max-w-5xl mt-8 border-2 border-dashed border-purple-300 dark:border-purple-700 bg-purple-50 dark:bg-purple-900/20 rounded-xl p-6 sm:p-8 md:p-12 text-center cursor-pointer transition-all hover:border-purple-500 dark:hover:border-purple-500 hover:bg-purple-100 dark:hover:bg-purple-900/30"> <div class="text-4xl sm:text-5xl md:text-6xl mb-4 text-purple-600 dark:text-purple-400">
📄
</div> <div class="text-lg sm:text-xl font-semibold text-zinc-800 dark:text-zinc-200 mb-2">
Arrastra tu archivo DOCX aquí
</div> <div class="text-xs sm:text-sm text-zinc-600 dark:text-zinc-400">
o haz clic para seleccionar
</div> <input type="file" id="file-input" accept=".docx" class="hidden"> </div> <!-- File Info --> <div id="file-info" class="flex hidden w-full max-w-5xl mt-4 bg-zinc-100 dark:bg-zinc-800 rounded-lg p-4 items-center justify-between cursor-pointer"> <span id="file-name" class="text-zinc-800 dark:text-zinc-200 font-medium"></span> <button id="remove-file" class="text-red-600 dark:text-red-400 hover:text-red-700 dark:hover:text-red-300 transition-colors cursor-pointer"> <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20"> <path fill-rule="evenodd" d="M4.293 4.293a1 1 0 011.414 0L10 8.586l4.293-4.293a1 1 0 111.414 1.414L11.414 10l4.293 4.293a1 1 0 01-1.414 1.414L10 11.414l-4.293 4.293a1 1 0 01-1.414-1.414L8.586 10 4.293 5.707a1 1 0 010-1.414z" clip-rule="evenodd"></path> </svg> </button> </div> <!-- Loading --> <div id="loading" class="hidden w-full max-w-5xl mt-8 text-center"> <div class="inline-block w-12 h-12 border-4 border-purple-200 dark:border-purple-800 border-t-purple-600 dark:border-t-purple-400 rounded-full animate-spin"></div> <p class="mt-4 text-zinc-600 dark:text-zinc-400">
Procesando archivo...
</p> </div> <!-- Success Message --> <div id="success-message" class="hidden items-center justify-center text-center w-full max-w-5xl mt-4 bg-green-50 dark:bg-green-900/20 border border-green-200 dark:border-green-800 text-green-800 dark:text-green-200 rounded-lg p-4">
✅ ¡Archivo convertido exitosamente!
</div> <!-- Error Message --> <div id="error-message" class="hidden w-full max-w-5xl mt-4 bg-red-50 dark:bg-red-900/20 border border-red-200 dark:border-red-800 text-red-800 dark:text-red-200 rounded-lg p-4"></div> <!-- Output Area --> <div id="output-area" class="hidden w-full max-w-5xl mt-8"> <!-- Stats --> <div class="grid grid-cols-1 sm:grid-cols-3 gap-4 mb-6"> <div class="bg-white dark:bg-zinc-800 rounded-lg p-6 text-center border border-zinc-200 dark:border-zinc-700"> <div id="question-count" class="text-2xl sm:text-3xl font-bold text-purple-600 dark:text-purple-400 mb-2">
0
</div> <div class="text-xs sm:text-sm text-zinc-600 dark:text-zinc-400">
Preguntas
</div> </div> <div class="bg-white dark:bg-zinc-800 rounded-lg p-6 text-center border border-zinc-200 dark:border-zinc-700"> <div id="correct-count" class="text-2xl sm:text-3xl font-bold text-green-600 dark:text-green-400 mb-2">
0
</div> <div class="text-xs sm:text-sm text-zinc-600 dark:text-zinc-400">
Respuestas correctas
</div> </div> <div class="bg-white dark:bg-zinc-800 rounded-lg p-6 text-center border border-zinc-200 dark:border-zinc-700"> <div id="answer-count" class="text-2xl sm:text-3xl font-bold text-blue-600 dark:text-blue-400 mb-2">
0
</div> <div class="text-xs sm:text-sm text-zinc-600 dark:text-zinc-400">
Respuestas totales
</div> </div> </div> <!-- Preview Section --> <div id="preview-section" class="bg-white dark:bg-zinc-800 rounded-lg py-6 mb-6 border border-zinc-200 dark:border-zinc-700"> <div class="text-base sm:text-lg font-semibold text-zinc-800 dark:text-zinc-200 mb-4 flex items-center gap-2 mx-3 sm:mx-6"> <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20"> <path d="M10 12a2 2 0 100-4 2 2 0 000 4z"></path> <path fill-rule="evenodd" d="M.458 10C1.732 5.943 5.522 3 10 3s8.268 2.943 9.542 7c-1.274 4.057-5.064 7-9.542 7S1.732 14.057.458 10zM14 10a4 4 0 11-8 0 4 4 0 018 0z" clip-rule="evenodd"></path> </svg>
Vista previa de preguntas procesadas:
</div> <div id="questions-preview" class="max-h-96 overflow-y-auto space-y-4"> <!-- Las preguntas se insertarán aquí dinámicamente --> </div> </div> <!-- GIFT Output --> <textarea id="gift-output" placeholder="El formato GIFT aparecerá aquí..." readonly class="w-full h-64 p-4 border border-zinc-300 dark:border-zinc-700 rounded-lg bg-zinc-50 dark:bg-zinc-900 text-zinc-900 dark:text-zinc-100 font-mono text-sm resize-none focus:outline-none focus:ring-2 focus:ring-purple-500"></textarea> <!-- Actions --> <div class="flex flex-col sm:flex-row gap-4 mt-4"> <button id="copy-btn" class="flex-1 cursor-pointer bg-zinc-600 hover:bg-zinc-700 text-white font-semibold py-3 px-6 rounded-lg transition-colors flex items-center justify-center gap-2"> <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20"> <path d="M8 3a1 1 0 011-1h2a1 1 0 110 2H9a1 1 0 01-1-1z"></path> <path d="M6 3a2 2 0 00-2 2v11a2 2 0 002 2h8a2 2 0 002-2V5a2 2 0 00-2-2 3 3 0 01-3 3H9a3 3 0 01-3-3z"></path> </svg>
Copiar al portapapeles
</button> <button id="download-btn" class="flex-1 cursor-pointer bg-purple-600 hover:bg-purple-700 text-white font-semibold py-3 px-6 rounded-lg transition-colors flex items-center justify-center gap-2"> <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20"> <path fill-rule="evenodd" d="M3 17a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1zm3.293-7.707a1 1 0 011.414 0L9 10.586V3a1 1 0 112 0v7.586l1.293-1.293a1 1 0 111.414 1.414l-3 3a1 1 0 01-1.414 0l-3-3a1 1 0 010-1.414z" clip-rule="evenodd"></path> </svg>
Descargar archivo .gift
</button> </div> </div> </div> </div> ` }), defineScriptVars({ commonScript }), defineScriptVars({ converterScript: docxConverterScript }));
}, "C:/dev/Astro/cla-tools/src/pages/tools/docx-gift.astro", void 0);

const $$file = "C:/dev/Astro/cla-tools/src/pages/tools/docx-gift.astro";
const $$url = "/tools/docx-gift";

const _page = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
  __proto__: null,
  default: $$DocxGift,
  file: $$file,
  url: $$url
}, Symbol.toStringTag, { value: 'Module' }));

const page = () => _page;

export { page };
